# Scripts

| Script | Role |
|--------|------|
| [compose-stack.sh](compose-stack.sh) | `docker compose` with `${GFS_HOST_DATA}/.env` |

```bash
export GFS_HOST_DATA=/home/gfs/gfs-data
./run/scripts/compose-stack.sh --help
```
