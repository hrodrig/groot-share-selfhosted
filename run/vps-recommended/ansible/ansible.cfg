[defaults]
# Pin interpreter per host in inventory.yml if you see discovery warnings, e.g.:
#   ansible_python_interpreter: /usr/bin/python3
interpreter_python = auto_silent

[privilege_escalation]
become = true
